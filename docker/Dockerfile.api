FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml ./
RUN pip install --no-cache-dir -e .

COPY src ./src
COPY configs ./configs
COPY data ./data
COPY models ./models

EXPOSE 8000

CMD ["uvicorn", "esg_model.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
